spring:
  application:
    name: kviz

  datasource:
    url: jdbc:postgresql://localhost:5432/kvizdb
    username: kviz
    password: kviz123

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    open-in-view: false

  flyway:
    enabled: true
    locations: classpath:db/migration

server:
  port: 8080